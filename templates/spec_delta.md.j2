# Spec delta: {{ first_change_capability }}

## ADDED Requirements

### Requirement: Initial {{ first_change_capability }} flow
The system SHALL support the initial `{{ first_change_capability }}` flow for {{ target_user | lower }}.

#### Scenario: User completes the target flow
- **GIVEN** the user needs to solve: {{ user_problem }}
- **WHEN** the user uses the `{{ first_change_capability }}` capability
- **THEN** the system enables the intended outcome
