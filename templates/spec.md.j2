# {{ capability }} Specification

## Purpose

Describe the expected behavior and boundaries for the `{{ capability }}` capability.

## Requirements

### Requirement: Core behavior
The system SHALL provide `{{ capability }}` capability behavior for its intended users.

#### Scenario: Basic success path
- **WHEN** a valid user action reaches `{{ capability }}`
- **THEN** the system responds with the expected result
